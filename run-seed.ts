import { seedSubscriptionPlans } from './server/seed-subscription-plans';

seedSubscriptionPlans()
  .then(() => {
    console.log('Done!');
    process.exit(0);
  })
  .catch((error) => {
    console.error('Error:', error);
    process.exit(1);
  });
